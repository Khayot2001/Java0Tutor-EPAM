import java.lang.Math;
public class Main
{
    static double func(double x, double y) {
        double c = (Math.sin(x) + Math.cos(y)) / (Math.cos(x) - Math.sin(y)) * Math.tan(x * y);
        return c;
    }
    
	public static void main(String[] args) {
	    System.out.println(func(6, 4));
	}
}
